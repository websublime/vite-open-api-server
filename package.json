{
  "name": "@websublime/vite-open-api-server-monorepo",
  "version": "0.0.0",
  "description": "Monorepo for vite-open-api-server plugin and playground",
  "keywords": [
    "vite",
    "plugin",
    "openapi",
    "mock-server",
    "monorepo"
  ],
  "license": "MIT",
  "author": "Websublime",
  "scripts": {
    "test:watch": "vitest",
    "lint": "biome check .",
    "typecheck": "pnpm build && pnpm -r --filter='./packages/*' exec tsc --noEmit",
    "build": "pnpm -r --filter='./packages/*' run build",
    "release": "workspace bump --execute --git-tag --git-push",
    "changeset": "workspace changeset create",
    "clean": "pnpm -r exec rm -rf dist node_modules",
    "test": "vitest run",
    "preinstall": "npx only-allow pnpm",
    "dev": "pnpm -r --parallel --filter='./packages/*' run dev",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "playground": "pnpm --filter petstore-app dev"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "@biomejs/biome": "^2.3.13",
    "vitest": "^4.0.18",
    "@types/node": "^25.2.0"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.0"
}
